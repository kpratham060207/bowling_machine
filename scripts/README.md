# Scripts

Utility scripts for development and operations will be added in later phases.

Phase 1A uses root `package.json` scripts and Docker Compose directly:

- `pnpm db:up` / `pnpm db:down` — PostgreSQL container
- `pnpm dev` — run app placeholders in parallel
