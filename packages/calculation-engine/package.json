{
  "name": "@bowling-machine/calculation-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Calibration-driven delivery calculation engine — Phase 1F",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run --config ../../vitest.config.ts src",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@bowling-machine/api-contracts": "workspace:*"
  },
  "devDependencies": {
    "@bowling-machine/config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
