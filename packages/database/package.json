{
  "name": "@bowling-machine/database",
  "version": "0.0.0",
  "private": true,
  "description": "Drizzle ORM schema, migrations, and database access — Phase 1B+",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.2"
  },
  "devDependencies": {
    "@bowling-machine/config": "workspace:*",
    "@types/node": "^22.10.2",
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.7.2"
  }
}
